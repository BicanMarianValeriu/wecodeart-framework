<?php
/**
 * WeCodeArt Framework
 *
 * WARNING: This file is part of the core WeCodeArt Framework. DO NOT edit this file under any circumstances.
 * Please do all modifications in the form of a child theme.
 *
 * @package		WeCodeArt Framework
 * @subpackage  Yoast\Blocks
 * @copyright   Copyright (c) 2023, WeCodeArt Framework
 * @since		6.1.2
 * @version		6.1.2
 */

namespace WeCodeArt\Support\Plugins\WooCommerce\Blocks;

defined( 'ABSPATH' ) || exit();

use WeCodeArt\Singleton;
use WeCodeArt\Gutenberg\Blocks\Dynamic;

use function add_action;
use function add_filter;
use function WeCodeArt\Functions\get_prop;

/**
 * Gutenberg Product Rating block.
 */
class Rating extends Dynamic {

	use Singleton;

	/**
	 * Block namespace.
	 *
	 * @var string
	 */
	protected $namespace = 'woocommerce';

	/**
	 * Block name.
	 *
	 * @var string
	 */
	protected $block_name = 'product-rating';

	/**
	 * Block args.
	 *
	 * @return 	array
	 */
	public function init() {
		add_filter( 'render_block_' . $this->get_block_type(), [ $this, 'render_filter' ], 20, 2 );
	}

    /**
	 * Filter Render
	 * 
	 * @param 	string 	$content
	 * 
	 * @return 	string
	 */
	public function render_filter( string $content = '', array $block = [] ): string {
		$is_not_query = get_prop( $block, [ 'attrs', 'isDescendentOfQueryLoop' ] ) === false;

		$regex = '/<div\b[^>]+wc-block-components-product-rating__stars[\s|"][^>]*>/U';
		if ( $is_not_query && 1 === preg_match( $regex, $content, $matches, PREG_OFFSET_CAPTURE ) ) {
			$offset  = $matches[0][1];
			$content = substr( $content, 0, $offset ) . $this->markup() . substr( $content, $offset );
		}

		return $content;
	}

	/**
	 * Markup
	 *
	 * @return 	string
	 */
	public function markup(): string {
		$html = '';

		global $product;

		if( ! is_object( $product ) ) {
			return $html;
		}

		$rating_count = $product->get_rating_count();

		if ( $rating_count === 0 || ! is_singular( 'product' ) ) {
			return $html;
		}

		$review_count = $product->get_review_count();
		$average      = $product->get_average_rating();

		ob_start();
		?> 
		<a href="#reviews" class="wc-block-components-product-rating__info" rel="nofollow">(<?php
			printf(
				_n( '%s customer review', '%s customer reviews', $review_count, 'wecodeart' ),
				'<span class="count">' . esc_html( $review_count ) . '</span>'
			);
		?>)</a>
		<p class="wc-block-components-product-rating__stats"><?php
		
			printf(
				__( '%s of the customers recommend the product', 'wecodeart' ),
				'<strong>' .  ( ( $average / 5 ) * 100 ) . '%</strong>'
			);
			
		?></p>
		<?php

		$html .= ob_get_clean();

		return $html;
	}
}
